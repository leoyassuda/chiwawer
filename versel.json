{
  "routes": [
    {
      "src": "/url/(?<id>[^/]+)",
      "methods": ["POST"],
      "dest": "/index.js/$id"
    },
    {
      "src": "/(?<id>[^/]+)",
      "methods": ["GET"],
      "dest": "/index.js/$id"
    }
  ]
}
